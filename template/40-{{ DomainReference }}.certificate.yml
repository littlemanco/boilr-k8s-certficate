---
# Generated by boilr at {{ time "Mon, 02 Jan 2006 15:04:05 MST" }}
apiVersion: "v1"
kind: "Certificate"
metadata:
  name: "{{ DomainReference  }}"
  namespace: "{{ Namespace }}"
spec:
  domain: "{{ Domain }}"
  email: "{{ Email }}"
  provider: "{{ Provider }}"
  secretName: "tls-{{ DomainReference }}"

